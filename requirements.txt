anthropic>=0.40.0
Pillow>=10.0.0
python-dotenv>=1.0.0

# CLIP training dependencies (only needed when running train_classifier.py or ClipCritic)
torch>=2.0.0
torchvision>=0.15.0
git+https://github.com/openai/CLIP.git
scikit-learn>=1.3.0
